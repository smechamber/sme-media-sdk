# SME Media SDK

Official SDK for SME Media Server.

## Install

npm install @sme/media-sdk

## Usage

```ts
import { MediaClient } from "@sme/media-sdk";

const media = new MediaClient({
  baseUrl: "...",
  token: "...",
});